#include <vector>
/**
 * @brief 
 * 
 * @param ar the sorted array where the key is searched
 * @param key the target element
 * @retval postive integer position of the element
 * @retval -1 element not found
 */
int BinarySearch(std::vector<int> ar, int key);